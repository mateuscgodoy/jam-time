import React from "react";

function Login() {
  return (
    <section className="login-section">
      <button>Login</button>
      <button>Logout</button>
    </section>
  );
}

export default Login;
